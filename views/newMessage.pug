extends layouts/main-layout.pug

block content    
    .chatPageContainer
        .chatTitleBar
            label.fw-bold.text-white(for="userSearchTextbox") To:
            #selectedUsers
                input#userSearchTextbox(type="text", placeholder="Type the name of the person")
        .resultsContainer
        button#createChatButton.tweet-btn(disabled) Create chat